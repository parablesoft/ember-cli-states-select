{{#x-select value=value id=id  onblur="onBlur"  class=class}}
{{x-option}}
{{#each stateList as |state|}}
	{{#x-option value=state.code}}
		{{#if onlyDisplayStateCode}}
				{{state.code}}
			{{else}}
				{{state.name}}
		{{/if}}
	{{/x-option}}
	{{/each}}
{{/x-select}}
